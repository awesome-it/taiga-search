server {
    listen 5173;
    root   /html/;
    index  index.html;
    location /index.html {
        expires off;
        etag off;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
