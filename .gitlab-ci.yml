include:

  - project: awesome/cicd
    file: common.yml
    ref: v1.0

  - project: awesome/cicd
    file: docker.yml
    ref: v1.0

variables:
  DOCKER_REGISTRY: registry.awesome-it.de
  DOCKER_USERNAME: gitlab-ci-token
  DOCKER_PASSWORD: $CI_JOB_TOKEN
